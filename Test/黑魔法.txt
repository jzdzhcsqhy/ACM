#include <stdio.h>

int main(void)
{
    int x = 5;
    printf("%d and ", sizeof(x++)); // note 1
    printf("%d\n", x); // note 2
    return 0;
}

这一段的输出结果是  4 and 5

注意到，虽然执行了sizeof(x++),但是输出的是后，x的值还是没有变。
因为sizeof是编译时行为，在运行时就不执行了。